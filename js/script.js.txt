// Este arquivo pode ser usado para adicionar interatividade ao seu site,
// como galerias de imagens, animações mais complexas ou validação de formulários.
// Para um portfólio estático simples, ele pode permanecer vazio por enquanto.
// Exemplo de como adicionar um "scroll suave" para âncoras (se você as usar):

/*
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();

        document.querySelector(this.getAttribute('href')).scrollIntoView({
            behavior: 'smooth'
        });
    });
});
*/
